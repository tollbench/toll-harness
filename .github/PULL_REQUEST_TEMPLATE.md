## What this changes

One or two sentences: what forced this change, and what it does.

## Checklist

- [ ] `ruff check .` and `pytest` pass locally
- [ ] Bug fixes carry a regression test that fails on the old code
- [ ] Behavior changes update the docs in the same PR
- [ ] No secrets, tokens, private hosts, or personal data in the diff
